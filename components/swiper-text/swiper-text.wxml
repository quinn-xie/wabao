<swiper class='scrollTips' vertical="true" autoplay="true" circular="true" interval="3000">
  <block wx:for="{{announcement}}" wx:key="{{index}}">
      <swiper-item>
        <view>{{item.title}}</view>
      </swiper-item>
  </block>
</swiper>
